Adapter Pattern

It is a pattern for connecting new code to legacy code without changing the working contract of legacy code.

This is useful when one portion of the application is legacy code and second part is new and not compatible with legacy code.

Client -> Adapter -> Adaptee

